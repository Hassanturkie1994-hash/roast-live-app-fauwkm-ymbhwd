
# Supabase Configuration
# These are PUBLIC client-safe variables that can be exposed in the client bundle
# The anon key is designed to be public and only provides row-level security access

# Your Supabase project URL
PUBLIC_SUPABASE_URL=https://uaqsjqakhgycfopftzzp.supabase.co

# Your Supabase anonymous (public) key
# This key is safe to expose in client-side code
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️ NEVER expose these in client code:
# - SUPABASE_SERVICE_ROLE_KEY (server-side only, used in Edge Functions)
# - Database connection strings
# - Private API keys
